<% content_for :title, 'Add event' %>
<% content_for :back_url, expense_events_url %>

<%= form_with model: @event do |form| %>
  <%= render partial: 'expense_events/event_details_form', locals: { form: } %>

  <%= form.submit "Create event", class: 'mt-4 btn btn-primary w-100' %>
<% end %>
